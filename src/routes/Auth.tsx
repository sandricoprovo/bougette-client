import styled from 'styled-components';

const Container = styled.div``;

export default function Auth() {
    return (
        <Container>
            <h1>Auth Page</h1>
        </Container>
    );
}
